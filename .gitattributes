# The committed contract JSON Schemas are generated by
# `python -m contracts.generate_schemas`, which always writes LF. Pin them to LF
# so a `core.autocrlf=true` (Windows) checkout does not rewrite them with CRLF
# and report every schema as drifted against the model-generated output.
contracts/schemas/*.json text eol=lf
